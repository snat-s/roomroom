[project]
name = "binoculars-env"
version = "0.1.0"
description = "Binoculars AI detection RL environment for verifiers"
requires-python = ">=3.12"
dependencies = [
    "verifiers>=0.1.8",
    "datasets>=3.0.0",
]
